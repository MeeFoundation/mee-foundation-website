---
import Layout from '@layouts/Layout.astro';
import {Header, Footer} from 'mee-components';
import {header, footer} from '../../astro.config.mjs';
import remoteControl from '../../public/images/index/remote-control.svg';
import privacyAndSecurity from '../../public/images/index/privacy-and-security.svg';
import privateSharing from '../../public/images/index/private-sharing.svg';
import yourDigitalTwin from '../../public/images/index/your-digital-twin.svg';
import playButton from '../../public/images/index/play-button.svg';
import Background from '@components/Background.astro';

const futures = [
  {
    image: remoteControl,
    title: 'Seamless form-free experience',
    description: 'You don’t have to fill in forms–passwords, names and more',
    class: 'col-span-2',
  },
  {
    image: yourDigitalTwin,
    title: 'Your data, your terms',
    description: 'Your data is never shared or sold by anyone',
    class: 'col-span-1',
  },
  {
    image: privateSharing,
    title: 'Empowered data control',
    description: 'Your legal right to control your own data is',
    class: 'col-span-1',
  },
  {
    image: privacyAndSecurity,
    title: 'Complete privacy, zero tracking',
    description:
      'Your activities remain private–no more tracking or spying on you',
    class: 'col-span-2',
  },
];
---

<Layout title="Mee Foundation">
  <Header header={header} type="dark" />

  <main class="h-screen overflow-hidden" id="main">
    <div class="relative h-0">
      <div
        id="background-container"
        class="relative z-200 flex transform-gpu items-center transition-all duration-800"
      >
        <Background />
        <div
          id="title"
          class:list={[
            'title top-0 z-70 flex h-screen w-full translate-x-0 transform-gpu items-center justify-center overflow-hidden px-10 pb-7 transition-all duration-800',
          ]}
        >
          <div
            class="flex w-full max-w-7xl flex-col items-center justify-center gap-4"
          >
            <div class="overflow-hidden">
              <h1
                class="animate-riseIn text-center text-3xl font-semibold text-white animation-delay-400 md:text-5xl lg:text-7xl"
              >
                Introducing a human-centered user experience for the internet
              </h1>
            </div>
            <div class="max-w-3xl overflow-hidden">
              <h3
                class="animate-riseIn text-center text-xl text-white animation-delay-400 lg:text-2xl"
              >
                Bringing the Mee experience to internet advertising, featuring a
                front-end component and back-end services
              </h3>
            </div>
          </div>
        </div>
      </div>
    </div>
    <article
      id="content-container"
      class="relative -z-10 mt-27 flex h-full w-screen flex-col items-center justify-between gap-8 overflow-hidden pt-16 md:gap-16"
    >
      <section class="flex max-w-7xl flex-col items-center px-6">
        <div class="relative flex w-full items-center">
          <img
            src="/images/index/video-preview.svg"
            alt="video"
            class="w-full rounded-3xl"
            id="video-preview"
          />
          <div
            class="absolute left-0 top-0 flex h-full w-full items-center justify-center"
          >
            <img
              src={playButton.src}
              alt="Play"
              class="cursor-pointer"
              id="play"
            />
          </div>
        </div>

        <video
          id="video"
          class="absolute -z-1 w-full rounded-3xl opacity-0"
          poster="/images/index/video-preview.svg"
          controls
          muted
          preload="metadata"
        >
          <source
            src="/videos/Mee-Foundation.webm"
            type="video/webm"
            media="(min-width: 600px)"
          />
          <source
            src="/videos/Mee-Foundation-mobi.webm"
            type="video/webm"
            media="(max-width: 599px)"
          />
          <source
            src="/videos/Mee-Foundation-mobi.mp4"
            type="video/mp4"
            media="(max-width: 599px)"
          />
          <source src="/videos/Mee-Foundation.webm" type="video/webm" />
          Video not supported.
        </video>
      </section>
      <section
        class="section top-0 z-70 mt-10 flex h-full w-screen items-center justify-center md:mt-20"
      >
        <div
          class="flex max-w-3xl flex-col items-center justify-center gap-4 px-6 transition-opacity duration-500"
        >
          <div class="flex flex-col items-center gap-1 overflow-hidden">
            <h1
              class="marker text-center text-4xl font-semibold text-grey-900 lg:text-7xl"
            >
              Mee experience values and principles
            </h1>
          </div>

          <div class="overflow-hidden">
            <h3
              class="text text-pretty px-6 text-center text-xl text-grey-900 animation-delay-800 md:text-2xl"
            >
              Bringing the Mee experience to internet, featuring a front-end
              component and back-end services
            </h3>
          </div>
        </div>
      </section>
      <section
        class="flex h-auto max-w-7xl flex-col items-center justify-center px-6"
      >
        <div class="overflow-hidden">
          <div class="">
            <div
              class="flex max-w-96 flex-wrap items-center justify-center gap-8 md:grid md:max-w-max md:grid-cols-2 lg:grid-cols-3"
            >
              {
                futures.map((future) => (
                  <div
                    class:list={[
                      'h-full rounded-2xl border border-grey-300',
                      future.class,
                    ]}
                  >
                    <img
                      src={future.image.src}
                      class="w-full"
                      alt={`${future.title}`}
                    />
                    <div class="flex flex-col gap-2 p-8 pt-0">
                      <div class="flex items-center gap-2 text-left text-xl font-semibold text-grey-900 lg:text-2xl">
                        {future.title}
                      </div>
                      <div class="text-sm text-grey-800 lg:text-base">
                        {future.description}
                      </div>
                    </div>
                  </div>
                ))
              }
            </div>
          </div>
        </div>
      </section>
      <section
        class="z-70 mb-20 flex h-full w-screen max-w-7xl flex-col items-center justify-center gap-10 md:gap-20"
      >
        <div
          class="flex flex-col items-center justify-center gap-4 px-6 transition-opacity duration-500"
        >
          <div class="overflow-hidden">
            <a
              class="block w-full px-6 text-center text-xl leading-relaxed text-brand-600 underline animation-delay-800 md:text-2xl"
              href="/projects"
            >
              Check our human-centered projects <i class="bi bi-arrow-right"
              ></i>
            </a>
          </div>
        </div>
      </section>
      <Footer footer={footer} mode="dark" />
    </article>
  </main>
</Layout>
<style is:global>
  .background {
    background: radial-gradient(#0d070c, #200e1d);
  }
  .shadow {
    position: absolute;
    top: 0;
    display: flex;
    justify-content: center;
    align-items: center;
    width: 100vw;
    height: 100vh;
    background: radial-gradient(#0f080e 20%, transparent 60%) center no-repeat;
    backface-visibility: hidden;
    transition: all 2s linear;
  }
  .background-container-hide {
    position: absolute;
    left: 0;
    right: 0;
    top: 0;
    width: 100vw;
    height: 100vh;
    overflow: hidden;
    -webkit-animation:
      slide 0.8s forwards,
      opacity 0.8s forwards;
    animation:
      slide 0.8s forwards,
      opacity 0.8s forwards;
    border-radius: 0px;
    opacity: 100%;
  }
  .background-container-show {
    position: absolute;
    left: var(--left-position);
    right: var(--left-position);
    width: var(--width-position);
    height: var(--height-position);
    top: 172px;
    overflow: hidden;
    -webkit-animation:
      slideBack 0.8s forwards,
      opacityBack 0.8s forwards,
      radiusBack 0.8s forwards;
    animation:
      slideBack 0.8s forwards,
      opacityBack 0.8s forwards,
      radiusBack 0.8s forwards;
    border-radius: 24px;
    opacity: 0%;
  }
  .background-container-hide .tunnel,
  .background-container-hide .figuresContainer {
    display: none;
  }
  .background-container-show .tunnel,
  .background-container-show .figuresContainer {
    display: none;
  }
  .background-container-hide #title {
    -webkit-animation: scale 0.8s forwards;
    animation: scale 0.8s forwards;
  }
  .background-container-show #title {
    -webkit-animation: scaleBack 0.8s forwards;
    animation: scaleBack 0.8s forwards;
  }

  @-webkit-keyframes slide {
    100% {
      left: var(--left-position);
      right: var(--left-position);
      width: var(--width-position);
      height: var(--height-position);
      top: 172px;
      border-radius: 24px;
    }
  }

  @keyframes slide {
    100% {
      left: var(--left-position);
      right: var(--left-position);
      width: var(--width-position);
      height: var(--height-position);
      top: 172px;
      border-radius: 24px;
    }
  }

  @-webkit-keyframes slideBack {
    100% {
      left: 0;
      right: 0;
      width: 100vw;
      height: 100vh;
      top: 0px;
      border-radius: 0px;
    }
  }

  @keyframes slideBack {
    100% {
      left: 0;
      right: 0;
      width: 100vw;
      height: 100vh;
      top: 0px;
      border-radius: 0px;
    }
  }
  @-webkit-keyframes opacity {
    70% {
      opacity: 100%;
    }
    80% {
      opacity: 75%;
    }
    90% {
      opacity: 60%;
    }
    95% {
      opacity: 50%;
    }
    100% {
      opacity: 0%;
    }
  }
  @keyframes opacity {
    70% {
      opacity: 100%;
    }
    80% {
      opacity: 75%;
    }
    90% {
      opacity: 60%;
    }
    95% {
      opacity: 50%;
    }
    100% {
      opacity: 0%;
    }
  }
  @-webkit-keyframes opacityBack {
    0% {
      opacity: 0%;
    }
    10% {
      opacity: 100%;
    }
    100% {
      opacity: 100%;
    }
  }

  @keyframes opacityBack {
    0% {
      opacity: 0%;
    }
    10% {
      opacity: 100%;
    }
    100% {
      opacity: 100%;
    }
  }

  @-webkit-keyframes radiusBack {
    95% {
      border-radius: 18px;
    }
    100% {
      border-radius: 0px;
    }
  }
  @keyframes radiusBack {
    90% {
      border-radius: 18px;
    }
    100% {
      border-radius: 0px;
    }
  }

  @-webkit-keyframes scale {
    0% {
      transform: scale(1);
    }
    100% {
      transform: scale(0.5);
    }
  }
  @keyframes scale {
    0% {
      transform: scale(1);
    }
    100% {
      transform: scale(0.5);
    }
  }
  @-webkit-keyframes scaleBack {
    0% {
      transform: scale(0.5);
    }
    100% {
      transform: scale(1);
    }
  }
  @keyframes scaleBack {
    0% {
      transform: scale(0.5);
    }
    100% {
      transform: scale(1);
    }
  }
</style>

<script>
  let fullscreenDisabled = true;
  const backgroundContainer = document.getElementById('background-container');
  const contentContainer = document.getElementById('content-container');
  const header = document.getElementById('header');
  const main = document.getElementById('main');
  interface NewHTMLVideoElement extends HTMLVideoElement {
    webkitRequestFullscreen(): Promise<void>;
    webkitEnterFullScreen(): Promise<void>;
    mozRequestFullScreen(): Promise<void>;
    msRequestFullscreen(): Promise<void>;
  }

  const video = document.getElementById('video') as NewHTMLVideoElement & {
    webkitRequestFullscreen(): Promise<void>;
    webkitEnterFullScreen(): Promise<void>;
  };

  video.addEventListener(
    'loadedmetadata',
    function () {
      fullscreenDisabled = false;
    },
    false,
  );

  const play = document.getElementById('play');
  const videoPreview = document.getElementById('video-preview');
  const setBGPositions = () => {
    const videoPreviewInfo = videoPreview?.getBoundingClientRect();
    const videoInfo = video?.getBoundingClientRect();

    const height = videoPreviewInfo?.height
      ? videoPreviewInfo?.height
      : videoInfo.height;
    const width = videoPreviewInfo?.width
      ? videoPreviewInfo?.width
      : videoInfo.width;
    const left = videoPreviewInfo?.left
      ? videoPreviewInfo?.left
      : videoInfo.left;
    document.documentElement.style.setProperty('--left-position', `${left}px`);
    document.documentElement.style.setProperty(
      '--height-position',
      `${height}px`,
    );
    document.documentElement.style.setProperty(
      '--width-position',
      `${width}px`,
    );
  };
  const makeFullScreen = (
    element: NewHTMLVideoElement & {
      webkitRequestFullscreen(): Promise<void>;
      webkitEnterFullScreen(): Promise<void>;
      mozRequestFullScreen(): Promise<void>;
      msRequestFullscreen(): Promise<void>;
    },
  ) => {
    if (!fullscreenDisabled) {
      if (element.mozRequestFullScreen) {
        element.mozRequestFullScreen();
      } else if (element.webkitRequestFullscreen) {
        element.webkitRequestFullscreen();
      } else if (element.webkitEnterFullScreen) {
        element.webkitEnterFullScreen();
      } else if (element.msRequestFullscreen) {
        element.msRequestFullscreen();
      } else {
        element.requestFullscreen();
      }
    }
  };

  play?.addEventListener('click', () => {
    if (window.innerWidth < 600) {
      video.classList.remove('hidden');
      video.play();
      setTimeout(() => makeFullScreen(video), 100);
    } else {
      videoPreview?.classList.add('hidden');
      play.parentElement?.classList.add('hidden');

      video.classList.remove('absolute', 'opacity-0', '-z-1');
      video.play();
    }
  });
  document.addEventListener('fullscreenchange', () => {
    if (!document.fullscreenElement) {
      video.pause();
    }
  });
  videoPreview?.addEventListener('load', () => {
    setBGPositions();
  });

  const hideBackgroundContainer = () => {
    const isContentHidden = contentContainer?.classList.contains('-z-10');
    if (isContentHidden) {
      setBGPositions();
      backgroundContainer?.classList.remove('background-container-show');
      backgroundContainer?.classList.add('background-container-hide');
      contentContainer?.classList.remove('-z-10');

      setTimeout(() => {
        header?.parentElement?.classList.remove('type-dark');
        header?.parentElement?.classList.add('type-default');
        header?.setAttribute('data-type', 'default');
      }, 300);

      backgroundContainer?.addEventListener('animationend', () => {
        const backgroundContainerHide = document.querySelector(
          '.background-container-hide',
        );
        if (backgroundContainerHide) {
          main?.classList.remove('h-screen');
          backgroundContainer?.classList.remove('z-200');
          backgroundContainer?.classList.add('-z-1');
        }
      });
    }
  };
  const showBackgroundContainer = () => {
    const isContentHidden = contentContainer?.classList.contains('-z-10');

    if (!isContentHidden) {
      video.pause();
      setBGPositions();
      contentContainer?.classList.add('-z-10');
      backgroundContainer?.classList.remove('-z-1');
      backgroundContainer?.classList.add('z-200');
      backgroundContainer?.classList.remove('background-container-hide');
      backgroundContainer?.classList.add('background-container-show');
      main?.classList.add('h-screen');
      header?.setAttribute('data-type', 'dark');
      header?.parentElement?.classList.add('type-dark');
      header?.parentElement?.classList.remove('type-default');

      backgroundContainer?.addEventListener('animationend', () => {
        const backgroundContainerShow = document.querySelector(
          '.background-container-show',
        );
        if (backgroundContainerShow) {
          backgroundContainer?.classList.remove('background-container-show');
        }
      });
    }
  };

  const onScroll = (event: WheelEvent) => {
    const swap = Math.sign(event.deltaY);
    if (swap === 1) {
      hideBackgroundContainer();
    } else if (!window.scrollY && swap === -1) {
      showBackgroundContainer();
    }
  };

  const isHeaderTouch = (event: TouchEvent) => {
    const element = event.target as HTMLElement;
    const header = element.closest('#header');
    const mobileHeader = element.closest('#mobile-header');
    return !!header || !!mobileHeader;
  };

  let startY = 0;
  let endY = 0;

  const onDragStart = (event: TouchEvent) => {
    if (!isHeaderTouch(event)) {
      startY = event.touches[0]?.clientY ?? 0;
    }
  };

  const onDragMove = (event: TouchEvent) => {
    if (!isHeaderTouch(event)) {
      endY = event.touches[0]?.clientY ?? 0;
    }
  };

  const onDragEnd = (event: TouchEvent) => {
    if (!isHeaderTouch(event)) {
      const swap = startY > endY ? 1 : -1;
      if (swap === 1) {
        hideBackgroundContainer();
      } else if (!window.scrollY && swap === -1) {
        showBackgroundContainer();
      }
    }
  };

  document?.addEventListener('touchstart', onDragStart);
  document?.addEventListener('touchmove', onDragMove);
  document?.addEventListener('touchend', onDragEnd);
  document.addEventListener('wheel', onScroll);
  addEventListener('resize', () => setBGPositions());
</script>
